import {IUser} from './../entity/User';

export let d0m_room1 : IUser = {
        name : 'd0m',
        room : 'room1', 
        uuid : 'd0m',
        alive : true,
        saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}


export let d0m_no_room : IUser = {
    name : 'd0m',
    room : '', 
    uuid : 'd0m',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let u1_room1 : IUser = {
    name : 'u1',
    room : 'room1',
    uuid : 'u1',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let u1_noroom : IUser = {
    name : 'u1',
    room : '',
    uuid : 'u1',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let u2_room1 : IUser = {
    name : 'u2',
    room : 'room1',
    uuid : 'u2',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let u2_noroom : IUser = {
    name : 'u2',
    room : '',
    uuid : 'u2',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let u3_room1 : IUser = {
    name : 'u3',
    room : 'room1',
    uuid : 'u3',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let jackNoRoom: IUser = {
    name : 'jack',
    room : '',
    uuid : 'jack',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let johnNoRoom: IUser = {
    name : 'john',
    room : '',
    uuid : 'john',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}

export let dam_room2 : IUser = {
    name : 'dam',
    room : 'room2',
    uuid : 'ew',
    alive : true,
    saveTetriBoard : Array(20).fill([]).map(() => Array(10).fill(0)),
}